#include <iostream>

#include "../includes/Standard.h"

int main()
{
    Log("Main Menu");
    Log("1. New Game");
    Log("2. Options");
    Log("3. Quit");
    std::string str = "Please Choose an Option";
    Log(str);
    Log(1);
    return 0;
}